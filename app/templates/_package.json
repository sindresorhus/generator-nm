{
  "name": "<%= moduleName %>",
  "version": "0.0.0",
  "description": "My <%= superb %> module",
  "license": "MIT",
  "repository": "<%= githubUsername %>/<%= moduleName %>",<% if (cli) { %>
  "bin": "cli.js",<% } %>
  "author": {
    "name": "<%= name %>",
    "email": "<%= email %>",
    "url": "<%= humanizedWebsite %>"
  },
  "engines": {
    "node": ">=0.10.0"
  },
  "scripts": {
    "test": "mocha"
  },
  "files": [
    "index.js"
  ],
  "keywords": [<% if (cli) { %>
    "cli-app",
    "cli",
    "bin",<% } %>
    ""
  ],
  "dependencies": {
    <% if (cli) { %>"meow": "^3.0.0"<% } %>
  },
  "devDependencies": {
    "mocha": "*"
  }
}
